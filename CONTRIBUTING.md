# Contributing

Bug reports and feature requests are welcome as issues.

This repository is a generated release artifact, so pull requests cannot be
merged here: the next release would overwrite them. If you have a patch,
attach it to an issue instead -- it can ship in a later release, with credit.
